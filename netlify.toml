[build]
  command = "npm install && npm run build"
  publish = "public"

[dev]
  command = "node server.js"
  port = 3000

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
