{
  "$schema": "https://open-railway.vercel.app/schema.json",
  "deploy": {
    "startCommand": "node server.js",
    "buildCommand": "npm install && npm run build",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
